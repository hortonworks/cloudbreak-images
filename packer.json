{
  "variables": {
    "mock": "{{ env `MOCK` }}",

    "aws_access_key": "{{ env `AWS_ACCESS_KEY` }}",
    "aws_secret_key": "{{ env `AWS_SECRET_ACCESS_KEY` }}",
    "gcp_account_file": "{{env `GCP_ACCOUNT_FILE`}}",
    "os_auth_url": "{{ env `OS_AUTH_URL` }}",
    "os_username": "{{ env `OS_USERNAME` }}",
    "os_password": "{{ env `OS_PASSWORD` }}",
    "os_tenant_name": "{{ env `OS_TENANT_NAME` }}",
	"sn" : "{{env `AZURE_SUBSCRIPTION_NAME`}}",
	"psPath" : "{{env `AZURE_PUBLISH_SETTINGS`}}",
    "client_id": "{{ env `ARM_CLIENT_ID` }}",
    "client_secret": "{{ env `ARM_CLIENT_SECRET` }}",
    "subscription_id": "{{ env `ARM_SUBSCRIPTION_ID` }}",
    "tenant_id": "{{ env `ARM_TENANT_ID` }}",
    "resource_group_name": "{{ env `ARM_GROUP_NAME` }}",
    "storage_account": "{{ env `ARM_STORAGE_ACCOUNT` }}",

    "os_user": "cloudbreak",
    "atlas_artifact": "cloudbreak",
    "image_name": "cloudbreak-{{ isotime \"2006-01-02\" }}{{env `IMAGE_NAME_SUFFIX` }}",
    "ssh_password":"S3cr3t",

    "instance_type": "m3.medium",

    "ami-eu-west-1": "ami-cfa412bc"
  },
  "builders":[
  {
    "name": "amazon",
    "type": "amazon-ebs",
    "region": "eu-west-1",
    "ami_regions": ["ap-southeast-1","ap-southeast-2","eu-central-1","ap-northeast-1","ap-northeast-2","us-east-1","sa-east-1","us-west-1","us-west-2"],
    "ssh_pty" : true,
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-eu-west-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "ec2-user",
    "ami_block_device_mappings": [{
                 "device_name": "/dev/sda1",
                 "volume_type": "gp2",
                 "delete_on_termination": true,
                 "volume_size": 15
            }],
    "launch_block_device_mappings": [{
                "device_name": "/dev/sda1",
                "volume_type": "gp2",
                "delete_on_termination": true,
                "volume_size": 15
            }],
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "{{ user `image_name`}}"
  },
  {
    "disk_size": "40",
    "name": "googlecompute",
    "type": "googlecompute",
    "account_file": "{{user `gcp_account_file`}}",
    "source_image": "docker-centos-2016-01-31",
    "zone": "us-central1-b",
    "project_id": "siq-haas",
    "ssh_username": "centos",
    "ssh_pty": "true",
    "machine_type": "n1-standard-2",
    "image_name": "{{user `image_name`}}",
    "tags": [
	"packer"
    ]
  },
    {
      "type": "openstack",
      "username": "{{user `os_username`}}",
      "password": "{{user `os_password`}}",
      "tenant_name": "{{user `os_tenant_name`}}",
      "ssh_username": "centos",
      "ssh_pty": "true",
      "image_name": "{{user `image_name`}}",
      "source_image": "c1632189-f509-4dba-ac1a-b08b07920b6d",
      "flavor": "3",
      "use_floating_ip": false,
      "networks": [
        "6414de5e-6085-4469-b8e9-aa293a65c828"
      ]
    }
    ,{
        "type": "azure-arm",

        "client_id": "{{user `client_id`}}",
        "client_secret": "{{user `client_secret`}}",
        "subscription_id": "{{user `subscription_id`}}",
        "tenant_id": "{{user `tenant_id`}}",

        "resource_group_name": "{{user `resource_group_name`}}",
        "storage_account": "{{user `storage_account`}}",

        "capture_container_name": "packer",
        "capture_name_prefix": "{{ user `image_name`}}",

        "custom_image_vhd_uri": "https://sequenceiqnortheurope2.blob.core.windows.net/system/Microsoft.Compute/Images/images/packer-osDisk.a095ebfd-43c8-4db7-9f9c-3fb590c47863.vhd",
        "ssh_pty": "true",
        "username": "{{user `os_user`}}",
        "ssh_password" :"{{user `ssh_password`}}",

        "location": "northeurope",
        "vm_size": "Standard_A2"
    }


  ],
  "provisioners": [
  {
    "type": "file",
    "source": "shared",
    "destination": "/tmp"
  },
  {
      "type": "shell",
      "execute_command": "sudo -S true <<< '{{ user `ssh_password`}}' ; chmod +x {{ .Path }}; {{ .Vars }} sudo -E bash '{{ .Path }}'",
      "scripts": ["./fix-waagent-sudo.sh"]
  },
  {
      "type": "shell",
      "inline": [
          "sudo yum install -y rsync",
          "sudo rsync -a /tmp/shared/ /"
      ]
  },
  {
    "type": "shell",
    "scripts": [
        "./pull-docker-images.sh",
        "./optimization-script-for-hdp.sh",
        "./cleanup.sh",
        "./create-gc-image.sh"
    ],
    "environment_vars": [
        "OS_USER={{ user `os_user` }}",
        "YUM_VERSION_DOCKER={{ user `yum_version_docker` }}",
        "YUM_VERSION_KERNEL={{ user `yum_version_kernel` }}",
        "YUM_VERSION_SYSTEMD={{ user `yum_version_systemd` }}",
        "PACKER_IMAGE_NAME={{user `image_name`}}",
        "IMAGES={{ user `cb_docker_container_ambari_server` }} {{ user `cb_docker_container_ambari_agent` }} {{ user `cb_docker_container_docker_consul_watch_plugn` }} {{ user `cb_docker_container_registrator` }} {{ user `cb_docker_container_logrotate` }} {{ user `cb_docker_container_kerberos` }} {{ user `cb_docker_container_ambari_db` }} {{ user `cb_docker_container_munchausen` }} {{ user `cb_docker_container_consul` }} {{ user `cb_docker_container_gateway` }} {{ user `cb_docker_container_alpine` }} {{ user `cb_docker_container_swarm` }} {{ user `cb_docker_container_cert_tool` }} {{ user `cb_docker_container_haveged` }} {{ user `cb_docker_container_ldap` }} {{ user `cb_docker_container_shipyard` }} {{user `cb_docker_container_shipyard_db`}} {{ user `cb_docker_container_list_others` }}",
        "TRACE=1"
    ],
    "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E bash '{{ .Path }}'"
  }
  ]
  ,"post-processors": [
    {
      "type": "atlas",
      "artifact": "sequenceiq/{{ user `atlas_artifact` }}",
      "artifact_type": "{{ build_name }}.image",
      "metadata": {
        "created_at": "{{timestamp}}",
        "created" :"{{ isotime \"2006-01-02 15:04:05 -0700\" }}",
        "cloudbreak_image_version": "{{ user `cloudbreak_image_version` }}",
        "os_user": "{{user `os_user` }}",
        "image_name": "{{ user `image_name` }}",
        "all_docker_images":"{{ user `cb_docker_container_ambari_server` }},{{ user `cb_docker_container_ambari_agent` }},{{ user `cb_docker_container_docker_consul_watch_plugn` }},{{ user `cb_docker_container_registrator` }},{{ user `cb_docker_container_logrotate` }},{{ user `cb_docker_container_kerberos` }},{{ user `cb_docker_container_ambari_db` }},{{ user `cb_docker_container_munchausen` }},{{ user `cb_docker_container_consul` }},{{ user `cb_docker_container_gateway` }},{{ user `cb_docker_container_alpine` }},{{ user `cb_docker_container_swarm` }},{{ user `cb_docker_container_cert_tool` }},{{ user `cb_docker_container_haveged` }},{{ user `cb_docker_container_ldap` }},{{ user `cb_docker_container_shipyard` }},{{user `cb_docker_container_shipyard_db`}},{{ user `cb_docker_container_list_others` }}"
      }
    }
  ]

}
