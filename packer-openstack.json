{
  "variables": {
    "os_auth_url": "",
    "os_image_name": "cb-centos71-amb210-{{ isotime \"2006-01-02\" }}",
    "os_username": "",
    "os_password": "",
    "os_tenant_name": ""
  },
  "builders": [
    {
      "type": "openstack",
      "username": "{{user `os_username`}}",
      "password": "{{user `os_password`}}",
      "project": "{{user `os_tenant_name`}}",
      "ssh_username": "centos",
      "provider": "{{user `os_auth_url`}}",
      "image_name": "{{user `os_image_name`}}",
      "source_image": "a9476eb0-58c6-4d56-9e79-513b93d86a64",
      "flavor": "3",
      "use_floating_ip": false,
      "networks": [
        "d6a01b5a-62a5-4151-88f0-718b575b680b"
      ]
    }
  ],
  "provisioners": [{
    "type": "file",
    "source": "scripts/",
    "destination": "/tmp"
  },
  {
    "type": "shell",
    "script": "./user-data-script.sh",
    "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E bash '{{ .Path }}'"
  },
  {
    "type": "shell",
    "script": "./optimization-script-for-hdp.sh",
    "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E bash '{{ .Path }}'"
  }]
}
