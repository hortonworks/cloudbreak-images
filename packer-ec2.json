{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "namesuffix": "{{env `IMAGE_NAME_SUFFIX`}}",
    "version": "0.9",
    "os_user": "",
    "instance_type": "m3.medium",
    "ami-sa-east-1": "ami-69fd4574",
    "ami-ap-northeast-1": "ami-f5ab40f5",
    "ami-eu-west-1": "ami-c554c6b2",
    "ami-us-east-1": "ami-48e8cc20",
    "ami-us-west-1": "ami-a7f91ee3",
    "ami-us-west-2": "ami-69220059",
    "ami-ap-southeast-2": "ami-87e697bd",
    "ami-ap-southeast-1": "ami-e8e3d5ba"
  },
  "builders":[{
    "name": "sa-east-1",
    "type": "amazon-ebs",
    "region": "sa-east-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-sa-east-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "ap-northeast-1",
    "type": "amazon-ebs",
    "region": "ap-northeast-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-ap-northeast-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "eu-west-1",
    "type": "amazon-ebs",
    "region": "eu-west-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-eu-west-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "us-east-1",
    "type": "amazon-ebs",
    "region": "us-east-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-us-east-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "us-west-1",
    "type": "amazon-ebs",
    "region": "us-west-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-us-west-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "us-west-2",
    "type": "amazon-ebs",
    "region": "us-west-2",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-us-west-2`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "ap-southeast-2",
    "type": "amazon-ebs",
    "region": "ap-southeast-2",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-ap-southeast-2`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "ap-southeast-1",
    "type": "amazon-ebs",
    "region": "ap-southeast-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-ap-southeast-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  }],
  "provisioners": [{
    "type": "file",
    "source": "scripts/",
    "destination": "/tmp"
  },
  {
    "type": "shell",
    "script": "./user-data-script.sh",
    "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E bash '{{ .Path }}'"
  }],
  "push": {
    "name": "sequenceiq/cloudbreak",
    "include": ["user-data-script.sh", "scrips/*"]
  }
}
