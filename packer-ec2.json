{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "namesuffix": "{{env `IMAGE_NAME_SUFFIX`}}",
    "version": "0.9",
    "os_user": "",
    "instance_type": "m3.medium",
    "ami-sa-east-1": "ami-7f56ef62",
    "ami-ap-northeast-1": "ami-0d9b720d",
    "ami-eu-west-1": "ami-ede0739a",
    "ami-us-east-1": "ami-0e80db66",
    "ami-us-west-1": "ami-777f9b33",
    "ami-us-west-2": "ami-3b1a390b",
    "ami-ap-southeast-2": "ami-11b0c12b",
    "ami-ap-southeast-1": "ami-36556364"
  },
  "builders":[
  {
    "name": "sa-east-1",
    "type": "amazon-ebs",
    "region": "sa-east-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-sa-east-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "ap-northeast-1",
    "type": "amazon-ebs",
    "region": "ap-northeast-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-ap-northeast-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "eu-west-1",
    "type": "amazon-ebs",
    "region": "eu-west-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-eu-west-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "us-east-1",
    "type": "amazon-ebs",
    "region": "us-east-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-us-east-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "us-west-1",
    "type": "amazon-ebs",
    "region": "us-west-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-us-west-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "us-west-2",
    "type": "amazon-ebs",
    "region": "us-west-2",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-us-west-2`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "ap-southeast-2",
    "type": "amazon-ebs",
    "region": "ap-southeast-2",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-ap-southeast-2`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  },
  {
    "name": "ap-southeast-1",
    "type": "amazon-ebs",
    "region": "ap-southeast-1",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "source_ami": "{{user `ami-ap-southeast-1`}}",
    "instance_type": "{{ user `instance_type` }}",
    "ssh_username": "{{ user `os_user` }}",
    "tags": {
      "builder": "packer",
      "version": "{{user `version`}}"
    },
    "ami_groups" :"all",
    "ami_name": "packer-cloudbreak-{{ isotime \"2006-01-02\" }}{{user `namesuffix`}}"
  }],
  "provisioners": [{
    "type": "file",
    "source": "scripts/",
    "destination": "/tmp"
  },
  {
    "type": "shell",
    "script": "./user-data-script.sh",
    "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E bash '{{ .Path }}'"
  }],
  "push": {
    "name": "sequenceiq/cloudbreak",
    "include": ["user-data-script.sh", "scrips/*"]
  }
}
